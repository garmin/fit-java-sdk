/////////////////////////////////////////////////////////////////////////////////////////////
// Copyright 2026 Garmin International, Inc.
// Licensed under the Flexible and Interoperable Data Transfer (FIT) Protocol License; you
// may not use this file except in compliance with the Flexible and Interoperable Data
// Transfer (FIT) Protocol License.
/////////////////////////////////////////////////////////////////////////////////////////////
// ****WARNING****  This file is auto-generated!  Do NOT edit this file.
// Profile Version = 21.201.0Release
// Tag = production/release/21.201.0-0-g39a0a60c
/////////////////////////////////////////////////////////////////////////////////////////////


package com.garmin.fit.csv;

import java.io.ByteArrayOutputStream; 
import java.io.OutputStream;

class PrefixableByteArrayOutputStream extends ByteArrayOutputStream implements ByteArrayOutputStreamPrefixable {
    private final ByteArrayOutputStream prefixStream;

    public PrefixableByteArrayOutputStream() {
        super();
        this.prefixStream = new ByteArrayOutputStream();
    }

    public PrefixableByteArrayOutputStream(int size) {
        super(size);
        this.prefixStream = new ByteArrayOutputStream();
    }

    @Override
    public void setPrefixStream(ByteArrayOutputStream stream) throws java.io.IOException {
        this.prefixStream.reset();
        stream.writeTo(prefixStream);
    }

    @Override
    public void close() throws java.io.IOException {
        super.close();
        prefixStream.close();
    }

    @Override
    public void reset() {
        super.reset();
        prefixStream.reset();
    }

    @Override
    public int size() {
        return super.size() + prefixStream.size();
    }

    @Override
    public byte[] toByteArray() {
        byte[] prefixBytes = prefixStream.toByteArray();
        byte[] mainBytes = this.buf;
        byte[] combinedBytes = new byte[prefixStream.size() + super.size()];
        System.arraycopy(prefixBytes, 0, combinedBytes, 0, prefixStream.size());
        System.arraycopy(mainBytes, 0, combinedBytes, prefixStream.size(), super.size());
        return combinedBytes;
    }

    @Override
    public String toString() {
        return new String(this.toByteArray());
    }

    @Override
    public String toString(String charsetName) throws java.io.UnsupportedEncodingException {
        return new String(this.toByteArray(), charsetName);
    }

    @Override
    public void writeTo(OutputStream out) throws java.io.IOException {
        prefixStream.writeTo(out);
        super.writeTo(out);
    }    
}